{
  "name": "resend-webhook",
  "version": "1.0.0",
  "main": "index.mjs",
  "type": "module",
  "license": "ISC",
  "description": "Traxent Resend webhook receiver (POST /webhooks/resend): Svix-verified unsubscribe, bounce, complaint and suppression events written back to the TraxentSubscribers table.",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@aws-sdk/client-ssm": "^3.700.0"
  }
}
