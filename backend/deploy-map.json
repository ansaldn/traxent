{
  "_comment": "Maps repo backend code to AWS resources that deploy-backend.yml updates in place. The payment functions (create-checkout, cancel-subscription, stripe-webhook) are intentionally NOT here — they're managed by the traxent-backend SAM stack via deploy-payments.yml, so deploy-backend no longer double-deploys them. Keys under 'lambdas' are folder names in backend/functions/.",
  "region": "eu-west-2",
  "lambdas": {},
  "cloudfrontFunctions": {
    "backend/cloudfront-functions/security-headers.js": "traxent-security-headers"
  }
}
