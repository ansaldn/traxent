{
  "_comment": "Maps repo backend code to the AWS resources the GitHub Actions backend deploy updates. Fill in the REPLACE_* values once with your real AWS names (see DEPLOY.md). Keys under 'lambdas' are the folder names in backend/functions/.",
  "region": "eu-west-2",
  "lambdas": {
    "cancel-subscription": "traxent-cancel-subscription",
    "create-checkout": "traxent-create-checkout",
    "stripe-webhook": "traxent-stripe-webhook"
  },
  "cloudfrontFunctions": {
    "backend/cloudfront-functions/security-headers.js": "traxent-security-headers"
  }
}
